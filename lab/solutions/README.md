# Solution guidance

These examples are reference implementations, not the only correct answers.
Attempt each exercise first, compare compiled SQL and test evidence, then record
why your approach differs. Files under `solutions/` are intentionally outside
the dbt `model-paths`, `test-paths`, and `macro-paths` so they do not alter the
working DAG.

